[package]
name = "horologium"
version = "2.2.1"
edition = "2021"
rust-version = "1.85"
description = "Cross-agent CLI status line and usage analytics in one fast binary"
license = "MIT"
repository = "https://github.com/Shallow-dusty/horologium"
readme = "README.md"
keywords = ["agent-cli", "statusline", "usage", "claude-code", "codex"]
categories = ["command-line-utilities"]

[[bin]]
name = "horologium"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
owo-colors = "4"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
rayon = "1"
toml = "0.8"

[dev-dependencies]
tempfile = "3"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
panic = "abort"
