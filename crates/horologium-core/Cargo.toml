[package]
name = "horologium-core"
version = "2.3.0"
description = "Horologium core: harness-agnostic pricing, record parsing, aggregation, and report formatting"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
anyhow.workspace = true
chrono.workspace = true
# clap is used only for `ValueEnum` derives on `Tier` / `CostMode`
# (deliberate decision: no Arg-wrapper enums, see AGENTS.md).
clap.workspace = true
rayon.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
tempfile.workspace = true
